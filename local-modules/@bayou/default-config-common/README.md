@bayou/default-config-common
============================

This module contains all the default implementations for injected configuration
which is common to both client and server sides of the system.
